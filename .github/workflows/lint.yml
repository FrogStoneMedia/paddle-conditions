name: Lint

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
      - run: pip install ruff
      - run: ruff check .
      - run: ruff format --check .
