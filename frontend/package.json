{
  "name": "paddle-conditions-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "chart.js": "^4.5.1",
    "esbuild": "^0.25.0"
  }
}
